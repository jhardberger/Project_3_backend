const express 		= require('express');
const app			= express();
const PORT 			= process.env.PORT || 3000;

//API CONNECTOR

//MIDDLEWARE

//REQUIRE CONTROLLERS

//USE MIDDLEWARE

app.listen(PORT, () => {
	console.log('listening on port: ' + PORT);
})